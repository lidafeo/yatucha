extends layout

include mixins/block

block content
    div#all-posts
        each post in posts
            +block(post)
    if posts.length == 10
        div.w-700
            button.btn.btn-lg.m-2.mb-3.btn-light.border.border-secondary.but-center#but-more(type="button") Загрузить еще
    if posts.length == 0
        p Нет постов
